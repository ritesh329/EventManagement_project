<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Registered Events</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(to right, #2193b0, #6dd5ed);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, sans-serif;
      color: #fff;
    }

    h1 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: bold;
      text-transform: uppercase;
    }

    .user-info {
      text-align: center;
      margin-bottom: 20px;
      font-size: 1.2rem;
    }

    .event-card {
      background: #fff;
      color: #333;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      transition: all 0.3s ease;
      height: 100%;
      display: flex;
      flex-direction: column;
    }

    .event-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
    }

    .event-image {
      position: relative;
      width: 100%;
      height: 180px;
      overflow: hidden;
    }

    .event-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.4s;
    }

    .event-card:hover .event-image img {
      transform: scale(1.05);
    }

    .event-title-overlay {
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      background: rgba(0, 0, 0, 0.6);
      color: #fff;
      padding: 8px 12px;
      font-size: 1.15rem;
      font-weight: bold;
    }

    .event-details {
      padding: 20px;
      flex-grow: 1;
    }
  </style>
</head>

<body>

  <div class="container py-5">
    <h1>My Registered Events</h1>

    <!-- User Info -->
    <div class="user-info">
      <p><strong>Name:</strong>
        <%= user.name %>
      </p>
      <p><strong>Email:</strong>
        <%= user.email %>
      </p>
    </div>

    <% if (registeredEvents.length===0) { %>
      <p class="text-center">You have not registered for any events yet.</p>
      <% } else { %>
        <div class="row g-4">
          <% registeredEvents.forEach(event=> { %>
            <div class="col-md-4">
              <div class="event-card">
                <div class="event-image">
                  <img src="<%= event.image || '/images/default.jpg' %>" alt="Event Image">
                  <div class="event-title-overlay">
                    <%= event.title %>
                  </div>
                </div>
                <div class="event-details">
                  <p><strong>Location:</strong>
                    <%= event.location %>
                  </p>
                  <p><strong>Date:</strong>
                    <%= new Date(event.date).toLocaleDateString('en-GB', {day:'2-digit',month:'short',year:'numeric'})
                      %>
                  </p>

                  <!-- Download Registration PDF Button -->
                  <% if (event.certificateUrl) { %>
                    <a href="<%= event.certificateUrl %>" target="_blank" class="btn btn-primary btn-sm w-100 mb-2">
                      Download Registration PDF
                    </a>
                    <% } %>

                      <!-- Cancel Registration Button -->
                      <form action="/api/events/<%= event.id %>/cancel" method="POST" style="margin:0;">
                        <button type="submit" class="btn btn-danger btn-sm w-100">
                          Cancel Registration
                        </button>
                      </form>
                </div>
              </div>
            </div>
            <% }); %>
        </div>
        <% } %>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>