<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= event.title %> - Details
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #f8f9fa, #e9ecef);
      min-height: 100vh;
      font-family: 'Segoe UI', Tahoma, sans-serif;
    }

    .event-card {
      max-width: 700px;
      margin: auto;
      background: #fff;
      border-radius: 1rem;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .event-img {
      height: 300px;
      width: 100%;
      object-fit: cover;
    }

    .event-header {
      background-color: #0d6efd;
      color: #fff;
      padding: 20px;
      text-align: center;
    }

    .event-body {
      padding: 25px;
    }

    .btn-custom {
      min-width: 150px;
    }
  </style>
</head>

<body>
  <div class="container py-5">
    <div class="event-card">
      <% if (event.image) { %>
        <img src="<%= event.image %>" alt="<%= event.title %>" class="event-img">
        <% } %>

          <div class="event-header">
            <h2 class="fw-bold">
              <%= event.title %>
            </h2>
          </div>

          <div class="event-body">
            <p class="mb-2"><strong>Location:</strong>
              <%= event.location %>
            </p>
            <p class="mb-2"><strong>Date:</strong>
            <p>
              <i class="fas fa-calendar-alt">
                <%= new Date(event.date).toLocaleString('en-GB', { weekday: 'short' , day: '2-digit' , month: 'short' ,
                  year: 'numeric' , hour: '2-digit' , minute: '2-digit' , hour12: true }) %>
              </i>
            </p>

            </p>
            <p class="mb-2"><strong>Capacity:</strong>
              <%= event.capacity %>
            </p>
            <p class="mb-2"><strong> description:</strong>
              <%= event.description %>
            </p>
            <p class="mb-4"><strong>Remaining Seats:</strong>
              <%= event.remainingCapacity %>
            </p>

            <div class="d-flex justify-content-center gap-3">
              <a href="/api/events/<%= event._id %>/register" class="btn btn-success btn-lg btn-custom">Register Now</a>
              <a href="/api/getAllevents" class="btn btn-outline-secondary btn-lg btn-custom">Back to Events</a>
            </div>
          </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>